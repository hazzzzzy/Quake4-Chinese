diff --git a/Q3E/src/main/jni/doom3/neo/CMakeLists.txt b/Q3E/src/main/jni/doom3/neo/CMakeLists.txt
index dadf7c48..40e43dce 100644
--- a/Q3E/src/main/jni/doom3/neo/CMakeLists.txt
+++ b/Q3E/src/main/jni/doom3/neo/CMakeLists.txt
@@ -391,6 +391,10 @@ include_directories(${OPENAL_INCLUDE_DIR})]]
 
 
 if(WIN32)
+	# GLES/KHR 等厂商头文件（纯头文件，游戏 DLL 也需要）
+	include_directories("${CMAKE_CURRENT_SOURCE_DIR}/externlibs/include")
+endif()
+if(WIN32 AND (CORE OR RAVEN OR HUMANHEAD OR SPLASHDAMAGE))
 	# skip SDL2main
 	if(APPLE OR WIN32)
 		set(SDL2_BUILDING_LIBRARY TRUE)
@@ -399,7 +403,6 @@ if(WIN32)
 	include_directories(${SDL2_INCLUDE_DIR})
 	set(SDLx_LIBRARY ${SDL2_LIBRARIES})
 	add_definitions(-D_SDL)
-	include_directories("${CMAKE_CURRENT_SOURCE_DIR}/externlibs/include")
 	set(sys_libs ${sys_libs} ${SDLx_LIBRARY})
 endif()
 
@@ -1230,6 +1233,7 @@ list(REMOVE_ITEM base_q4game_src
 list(APPEND base_q4game_src
 		Effect.cpp
 		FreeView.cpp
+		Subtitles.cpp
 		Game_Debug.cpp
 		Game_Log.cpp
 		Healing_Station.cpp
diff --git a/Q3E/src/main/jni/doom3/neo/quake4/Game_local.cpp b/Q3E/src/main/jni/doom3/neo/quake4/Game_local.cpp
index fbb6731a..3e70a73c 100644
--- a/Q3E/src/main/jni/doom3/neo/quake4/Game_local.cpp
+++ b/Q3E/src/main/jni/doom3/neo/quake4/Game_local.cpp
@@ -1,6 +1,8 @@
-#include "../idlib/precompiled.h"
+﻿#include "../idlib/precompiled.h"
 #pragma hdrstop
 
+#include "Subtitles.h"
+
 #include "Game_local.h"
 
 // RAVEN BEGIN
@@ -4013,6 +4015,9 @@ bool idGameLocal::Draw( int clientNum ) {
 	gameDebug.DrawHud( );
 // RAVEN END
 
+	// karin: 汉化项目 —— 语音字幕（含过场，始终绘制在最上层）
+	gameSubtitles.Draw();
+
 	return true;
 }
 
diff --git a/Q3E/src/main/jni/doom3/neo/quake4/LipSync.cpp b/Q3E/src/main/jni/doom3/neo/quake4/LipSync.cpp
index 2854fcf0..a3bd62c3 100644
--- a/Q3E/src/main/jni/doom3/neo/quake4/LipSync.cpp
+++ b/Q3E/src/main/jni/doom3/neo/quake4/LipSync.cpp
@@ -1,7 +1,8 @@
-#include "../idlib/precompiled.h"
+﻿#include "../idlib/precompiled.h"
 #pragma hdrstop
 
 #include "Game_local.h"
+#include "Subtitles.h"
 
 // 1	normal
 // 2	scared
@@ -386,9 +387,13 @@ int idAFAttachment::StartLipSyncing( const char *speechDecl )
 		}
 
 		// Start the associated sound
-		refSound.diversity = 0.0f; 
+		refSound.diversity = 0.0f;
 		renderEntity.referenceSoundHandle = refSound.referenceSoundHandle;
-		StartSoundShader( declManager->FindSound( lipSync->GetName() ), SND_CHANNEL_VOICE, refSound.parms.soundShaderFlags | SSF_IS_VO, false, &length );
+		const idSoundShader *voShader = declManager->FindSound( lipSync->GetName() );
+		StartSoundShader( voShader, SND_CHANNEL_VOICE, refSound.parms.soundShaderFlags | SSF_IS_VO, false, &length );
+
+		// karin: 汉化项目 —— 语音字幕（文本经 GetTranscribeText 自动本地化，按声音衰减做可听性门控）
+		gameSubtitles.AddFromEntity( GetBody(), lipSync->GetTranscribeText(), length, voShader );
 
 		// Start the default emotion
 		anim = "emotion_";
diff --git a/Q3E/src/main/jni/doom3/neo/quake4/Misc.cpp b/Q3E/src/main/jni/doom3/neo/quake4/Misc.cpp
index 5c446402..02958f6c 100644
--- a/Q3E/src/main/jni/doom3/neo/quake4/Misc.cpp
+++ b/Q3E/src/main/jni/doom3/neo/quake4/Misc.cpp
@@ -1,4 +1,4 @@
-/*
+﻿/*
 
 Various utility objects and functions.
 
@@ -16,6 +16,7 @@ Various utility objects and functions.
 #endif
 #include "ai/AI.h"
 // RAVEN END
+#include "Subtitles.h"
 
 /*
 ===============================================================================
@@ -4026,6 +4027,17 @@ void idFuncRadioChatter::Event_Activate( idEntity *activator ) {
 		}
 		player->StartSoundShader( shader, SND_CHANNEL_RADIO, SSF_GLOBAL, false, &length );
 		time = MS2SEC( length + 150 );
+
+		// karin: 汉化项目 —— 无线电通讯字幕（shader 与 lipsync decl 同名，全局声不做可听性门控）
+		// 注意：部分纯无线电台词原版无口型需求，Raven 未建 lipsync decl（如 airdefense1 的 vo_1_1_1_68_1），
+		// 此类台词无文本来源，字幕缺失属资产缺口而非代码缺陷
+		const rvDeclLipSync *radioLipSync = declManager->FindLipSync( sound, false );
+		if ( radioLipSync ) {
+			// 来源前缀"无线电"（UTF-8 转义，2026-07-18 用户要求字幕都有来源）
+			gameSubtitles.Add( "\xE6\x97\xA0\xE7\xBA\xBF\xE7\x94\xB5", radioLipSync->GetTranscribeText(), length );
+		} else if ( harm_g_subtitleDebug.GetBool() ) {
+			gameLocal.Printf( "[SUB] radio: no lipsync decl for '%s'\n", sound );
+		}
 	}
 
 	if ( player && player->GetHud() ) {
diff --git a/Q3E/src/main/jni/doom3/neo/quake4/Player.cpp b/Q3E/src/main/jni/doom3/neo/quake4/Player.cpp
index f755e134..9ac08cb2 100644
--- a/Q3E/src/main/jni/doom3/neo/quake4/Player.cpp
+++ b/Q3E/src/main/jni/doom3/neo/quake4/Player.cpp
@@ -1,4 +1,4 @@
-// RAVEN BEGIN
+﻿// RAVEN BEGIN
 // bdube: note that this file is no longer merged with Doom3 updates
 //
 // MERGE_DATE 09/30/2004
@@ -7,6 +7,7 @@
 #pragma hdrstop
 
 #include "Game_local.h"
+#include "../ui/Window.h"
 
 #include "ai/AI.h"
 #include "ai/AI_Manager.h"
@@ -21,8 +22,6 @@
 // RAVEN BEGIN
 // nrausch: support for turning the weapon change ui on and off
 #ifdef _XENON
-#include "../ui/Window.h"
-
 // nrausch: support for direct button input
 #include "../sys/xenon/xen_input.h"
 #endif
@@ -30,6 +29,19 @@
 #ifdef _MOD_VIEW_BODY
 #include "ViewBody.cpp"
 #endif
+
+static void ClearSaveMessageGui( idUserInterface *gui ) {
+	if ( !gui || !gui->GetDesktop() ) {
+		return;
+	}
+
+	idWinVar *visible = gui->GetDesktop()->GetWinVarByName( "quicksave_msg::visible", true );
+	if ( visible ) {
+		visible->Set( "0" );
+		visible->SetEval( false );
+	}
+}
+
 #ifdef _QUAKE4 //karin: allow fire when focus NPC
 static idCVar harm_g_allowFireWhenFocusNPC( "harm_g_allowFireWhenFocusNPC", "0", CVAR_GAME | CVAR_BOOL | CVAR_ARCHIVE, "allow fire when focus NPC" );
 #endif
@@ -2457,10 +2469,14 @@ void idPlayer::Restore( idRestoreGame *savefile ) {
 	weaponViewModel.Restore( savefile );	
 	weaponWorldModel.Restore( savefile );	
 
- 	savefile->ReadUserInterface( hud, &spawnArgs );	
+	savefile->ReadUserInterface( hud, &spawnArgs );
 	assert( !mphud );									// Don't save MP stuff
 	savefile->ReadUserInterface( objectiveSystem, &spawnArgs );
 	savefile->ReadUserInterface( cinematicHud, &spawnArgs );
+	// "游戏已保存"是瞬时提示，不应随 GUI 状态从旧存档恢复；否则它没有
+	// 对应的 SaveMessage 计时起点，现有 watchdog 无法将其清除。
+	ClearSaveMessageGui( hud );
+	ClearSaveMessageGui( objectiveSystem );
 	savefile->ReadBool( objectiveSystemOpen );
 
 #ifdef _XENON
@@ -9449,6 +9465,7 @@ Called every tic for each player
 ==============
 */
 void idPlayer::Think( void ) {
+	SaveMessageWatchdog();
 #ifdef _MOD_FULL_BODY_AWARENESS
 	if(!harm_pm_fullBodyAwareness.GetBool() || pm_thirdPerson.GetBool() || IsInVehicle() || zoomed || focusUI)
 	{
@@ -13821,6 +13838,12 @@ void idPlayer::SetHudOverlay( idUserInterface* overlay, int duration ) {
 idPlayer::SaveMessage
 =====================
 */
+// 2026-07-18 用户反馈"游戏已保存有时不消失"：hud.gui 里 onNamedEvent saveMessage
+// 设 quicksave_msg::visible=1 + onTime 3000 ms 自清；但游戏 pause/loading 期间
+// gui 时间线可能被跳过 → onTime 不触发 → 提示卡住。DLL 侧记录最近触发时刻，
+// Think 里 4 秒后强制清 gui state 兜底。
+static int g_saveMessageStartMs = 0;
+
 void idPlayer::SaveMessage( void ) {
 #ifndef _XENON
 	if ( GetHud( ) ) {
@@ -13830,6 +13853,18 @@ void idPlayer::SaveMessage( void ) {
 	if ( objectiveSystem ) {
 		objectiveSystem->HandleNamedEvent( "saveMessage" );
 	}
+	g_saveMessageStartMs = gameLocal.time;
+#endif
+}
+
+// 强制清 quicksave_msg 兜底（Think 每帧调用）
+void idPlayer::SaveMessageWatchdog( void ) {
+#ifndef _XENON
+	if ( g_saveMessageStartMs && gameLocal.time - g_saveMessageStartMs > 4000 ) {
+		ClearSaveMessageGui( GetHud() );
+		ClearSaveMessageGui( objectiveSystem );
+		g_saveMessageStartMs = 0;
+	}
 #endif
 }
 
diff --git a/Q3E/src/main/jni/doom3/neo/quake4/Player.h b/Q3E/src/main/jni/doom3/neo/quake4/Player.h
index 31d63795..0230ad9f 100644
--- a/Q3E/src/main/jni/doom3/neo/quake4/Player.h
+++ b/Q3E/src/main/jni/doom3/neo/quake4/Player.h
@@ -1,4 +1,4 @@
-// RAVEN BEGIN
+﻿// RAVEN BEGIN
 // bdube: note that this file is no longer merged with Doom3 updates
 //
 // MERGE_DATE 07/07/2004
@@ -799,6 +799,8 @@ public:
 
 	// mekberg: wrap saveMessages
 	void					SaveMessage( void );
+	// 2026-07-18 用户反馈 quicksave_msg 有时不消失：Think 里每帧调用兜底强制清
+	void					SaveMessageWatchdog( void );
 
 	// mekberg: set pm_ cvars
 	void					SetPMCVars( void );
diff --git a/Q3E/src/main/jni/doom3/neo/quake4/Sound.cpp b/Q3E/src/main/jni/doom3/neo/quake4/Sound.cpp
index 5c9d2262..051cc101 100644
--- a/Q3E/src/main/jni/doom3/neo/quake4/Sound.cpp
+++ b/Q3E/src/main/jni/doom3/neo/quake4/Sound.cpp
@@ -1,8 +1,9 @@
-
+﻿
 #include "../idlib/precompiled.h"
 #pragma hdrstop
 
 #include "Game_local.h"
+#include "Subtitles.h"
 
 /*
 ===============================================================================
@@ -251,6 +252,25 @@ idSound::DoSound
 void idSound::DoSound( bool play ) {
 	if ( play ) {
 		StartSoundShader( refSound.shader, SND_CHANNEL_ANY, refSound.parms.soundShaderFlags, true, &playingUntilTime );
+
+		// karin: 汉化项目 —— speaker 实体播报字幕（基地广播 PA 等，2026-07-17
+		// 用户反馈开场女声广播缺字幕）。同无线电：按 sound shader 名找 lipsync
+		// decl（兼容 vo_* 直名与 aiSpeak 的 lipsync_ 前缀名），无 decl 的环境音
+		// speaker 自然不受影响；可听性门控走 AddFromEntity（距离/过场规则）
+		if ( refSound.shader && playingUntilTime > 0 ) {
+			const char *sndName = refSound.shader->GetName();
+			const rvDeclLipSync *ls = declManager->FindLipSync( sndName, false );
+			if ( !ls ) {
+				ls = declManager->FindLipSync( va( "lipsync_%s", sndName ), false );
+			}
+			if ( ls ) {
+				// 兜底来源前缀"广播"（UTF-8 转义，2026-07-18 用户要求字幕都有来源）
+				gameSubtitles.AddFromEntity( this, ls->GetTranscribeText(), playingUntilTime, refSound.shader, "\xE5\xB9\xBF\xE6\x92\xAD" );
+			} else if ( harm_g_subtitleDebug.GetBool() ) {
+				gameLocal.Printf( "[SUB] speaker: no lipsync decl for '%s'\n", sndName );
+			}
+		}
+
 		playingUntilTime += gameLocal.time;
 	} else {
 		StopSound( SND_CHANNEL_ANY, true );
diff --git a/Q3E/src/main/jni/doom3/neo/quake4/ai/AI.cpp b/Q3E/src/main/jni/doom3/neo/quake4/ai/AI.cpp
index 8fce0d71..fc0d2ccc 100644
--- a/Q3E/src/main/jni/doom3/neo/quake4/ai/AI.cpp
+++ b/Q3E/src/main/jni/doom3/neo/quake4/ai/AI.cpp
@@ -1,4 +1,4 @@
-/*
+﻿/*
 ================
 
 AI.cpp
@@ -17,6 +17,7 @@ AI.cpp
 #include "../Projectile.h"
 #include "../spawner.h"
 #include "AI_Tactical.h"
+#include "../Subtitles.h"
 
 const char* aiTalkMessageString [ ] = {
 	"None",
@@ -4511,8 +4512,17 @@ bool idAI::Speak( const char *lipsync, bool random ){
 	if ( head ) {
 		speakTime = head->StartLipSyncing( lipsync );
 	} else {
-		speakTime = 0;		
-		StartSoundShader (declManager->FindSound ( lipsync ), SND_CHANNEL_VOICE, SSF_IS_VO, false, &speakTime );
+		speakTime = 0;
+		const idSoundShader *voShader = declManager->FindSound ( lipsync );
+		StartSoundShader ( voShader, SND_CHANNEL_VOICE, SSF_IS_VO, false, &speakTime );
+
+		// karin: 汉化项目 —— 无头模型 AI 不走 StartLipSyncing，字幕在此注入
+		const rvDeclLipSync *voLipSync = declManager->FindLipSync( lipsync, false );
+		if ( voLipSync ) {
+			gameSubtitles.AddFromEntity( this, voLipSync->GetTranscribeText(), speakTime, voShader );
+		} else if ( harm_g_subtitleDebug.GetBool() ) {
+			gameLocal.Printf( "[SUB] headless: no lipsync decl for '%s'\n", lipsync );
+		}
 	}
 
 	speakTime += gameLocal.time;
